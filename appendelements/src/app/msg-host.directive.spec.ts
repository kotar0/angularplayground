import { MsgHostDirective } from './msg-host.directive';

describe('MsgHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MsgHostDirective();
    expect(directive).toBeTruthy();
  });
});
